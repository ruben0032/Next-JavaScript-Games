/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media.karousell.com', 'assetsio.reedpopcdn.com', 'image.jeuxvideo.com', 'rogueco.com'],
    },
}

module.exports = nextConfig